<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<title>
			<%= @u.login %>
		</title>
		<link rel="stylesheet" href="/style.css" />
	</head>
	<body class="body_index">
		<div class="header_index">
			<div class="header_img">
				<img src="/ruby.png" width="100px" height="100px" alt="logo" />
			</div>
			<div class="header_text">
			Authentification service
			</div>
		</div>
		<div class="effect">&nbsp;</div>
		<div class="index_content">
			<div class="box">
				<h1>Hello <%= @u.login %></h1>
				
				<div class="index_appli">
				<div class="disconnect"><a href="/session/destroy">disconnect</a></div>
					<div class="appli_title">
						<h2>Your Applications</h2>
					</div>
					<div class="appli_img">
						<a href="appli/new"><img src="/add.png" alt="add an application" /></a>
					</div>
				</div>
				<p class="clear"></p>
				<% if @Dapp && !@Dapp.empty? %>
				<ul>
				  <% @Dapp.each do |app| %>
				  <li><%= app.name %>&nbsp;<a href="appli/<%= app.name %>/destroy"><img src="delete.png" alt="delete the application" /></a></li>
				  <% end %>
				</ul>
				<% else %>
				 You have not add an application.
				<% end %>
				<br /><br />
				<h2>Applications who use your Account</h2>
				  <% if @Uapp && !@Uapp.empty? %>
				<ul>
  				  <% @Uapp.each do |app| %>
				  <li><%= app.name %></li>
				  <% end %>
				</ul>
				<% else %>
				  No applications use your account.
				<% end %>
			</div>
		</div>
	</body>
</html>	
